# Changelog

## 0.1.0

Initial release: status bar item with active project + task progress, Quick Pick menu (open tasks/context files, dashboard, switch project), update-available badge, live refresh on MissionCache file changes.
